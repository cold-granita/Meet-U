package com.example.meetu_application.android.ndefemulation

object BuildConfig {
    // Livello di log minimo da mostrare (VERBOSE, DEBUG, INFO, WARN, ERROR)
    const val LOG_LEVEL = android.util.Log.VERBOSE

    // Abilita o disabilita i log
    const val LOGS_ENABLED = true

    // Tag usato nei log
    const val LOG_TAG = "NdefEmulation"
}